# simplec — настройки БД
# Выберите бэкенд (на будущее для приложения):
SIMPLEC_STORAGE_BACKEND=postgres  # postgres|sqlite

# PostgreSQL (используются psql и скрипты pg_*):
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=simplec

# Для ORM в дальнейшем:
SIMPLEC_DB_URL=postgresql+psycopg://postgres:postgres@localhost:5432/simplec

# Если нужен SQLite как fallback:
SIMPLEC_DB_PATH=var/simplec.db
# SIMPLEC_DB_URL=sqlite:///var/simplec.db
