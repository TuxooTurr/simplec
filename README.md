
# SimpleC

Автоматизированный генератор тест-кейсов для Zephyr на FastAPI с интеграцией GigaChat (Sber) и OpenAI (в разработке).  
Разработан для быстрой генерации, хранения и экспорта тест-кейсов, а также для работы с корпоративными LLM по API.

---

## ⚡ Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/TuxooTurr/simplec.git
cd simplec
```

### 2. Настройка окружения

**Создайте виртуальное окружение и установите зависимости:**

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### 3. Запуск приложения

```bash
python -m uvicorn simplec.web.app:app --reload --port 8080
```

**Откройте в браузере:**  
[http://localhost:8080](http://localhost:8080)

---

## 🚀 Основные возможности

- Генерация тест-кейсов с помощью GigaChat API (Sber)
- Экспорт тест-кейсов для Zephyr
- Web-интерфейс на FastAPI & Jinja2
- Использование промптов/темплейтов для разных LLM
- Поддержка командной работы (git)

---

## 🛠️ Структура проекта

```
simplec/
│
└── web/
    ├── app.py             # Entrypoint
    ├── templates/
    │     └── index.html   # Основной шаблон UI
    └── static/            # (при необходимости) статика для фронта
.gitignore
requirements.txt
README.md
```

---

## 🤖 GigaChat API

> Для работы GigaChat необходимы актуальные ACCESS ключи и Client Secret.  
> **.env/.secrets не размещаются в git!**  
>  
> Получение токена подробно [описано тут](https://developers.sber.ru/docs/ru/gigachat/quickstart/legal-using-api).  
>  
> Используйте скрипт `gigachat_token.py` для генерации и автоматического обновления токенов.

---

## 📝 Пример .gitignore

```gitignore
.venv/
__pycache__/
.idea/
.vscode/
.env
*.pyc
*.sqlite3
```

---

## 🗂 Перенос/установка на другой компьютер

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/TuxooTurr/simplec.git
   cd simplec
   ```
2. Запустите виртуальное окружение и установите зависимости:
   ```bash
   python3 -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt
   ```
3. Запустите сервер:
   ```bash
   python -m uvicorn simplec.web.app:app --reload --port 8080
   ```
4. Откройте `localhost:8080` в браузере.

---

## ⛔️ Безопасность

**Никогда не коммитьте в репозиторий:**
- .env файлы с токенами/key
- Сырые ключи и пароли API

---

## 📋 TODO

- [ ] Интеграция с OpenAI API
- [ ] Улучшенный UI
- [ ] Unit тесты для генерации тест-кейсов
- [ ] Документация по REST API

---

## 📞 Контакты & поддержка

Мейнтейнер: [TuxooTurr](https://github.com/TuxooTurr)  
Пишите вопросы прямо в issues или открывайте PR!
