# SimpleC

**AI-powered генератор тест-кейсов для Jira Zephyr**

Автоматическое создание детальных тест-кейсов из бизнес-требований с использованием LLM.

---

## 🎯 Основные возможности

- **3-слойная генерация:**
  1. QA документация (описание, потоки данных, API, БД, бизнес-правила)
  2. Список тест-кейсов с приоритетами
  3. Детальные шаги для каждого кейса (UI/API/БД уровни)

- **Автоопределение типа требований:** API, UI, Бизнес-логика, Интеграции, Безопасность

- **Гибкая глубина тестирования:** Smoke (1-5) → Regression (5-10) → Full (11-30) → Atomary (31-100)

- **Экспорт в Jira Zephyr:** XML, CSV, Markdown с полной настройкой метаданных

- **Поддержка нескольких LLM:** GigaChat, DeepSeek, Ollama, LM Studio

- **База эталонов:** ChromaDB для улучшения качества генераций

- **Парсинг файлов:** PDF, DOCX, XLSX, XML, изображения

---

## 🚀 Установка и запуск

```bash
# Клонировать репозиторий
git clone https://github.com/TuxooTurr/simplec.git
cd SimpleC

# Создать виртуальное окружение
python3 -m venv .venv
source .venv/bin/activate  # macOS/Linux
# .venv\Scripts\activate   # Windows

# Установить зависимости
pip install -r requirements.txt

# Настроить переменные окружения
cp .env.example .env
# Отредактировать .env, добавить API ключи

# Запустить приложение
streamlit run app.py
```

Приложение откроется на `http://localhost:8501`

---

## 🛠 Технологии

- **Frontend:** Streamlit (Neumorphism dark theme)
- **LLM:** GigaChat, DeepSeek, Ollama, LM Studio
- **Vector DB:** ChromaDB
- **Парсинг:** PyPDF2, python-docx, openpyxl, Pillow
- **Export:** XML (Zephyr), CSV, Markdown

---

## 📁 Структура проекта

```
SimpleC/
├── app.py                      # Streamlit UI
├── tc_formatter.py             # Парсер/форматтер XML
├── requirements.txt            # Зависимости
├── .env                        # API ключи (не в git)
├── agents/
│   ├── llm_client.py          # Универсальный LLM клиент
│   ├── layered_generator.py   # 3-слойный генератор
│   ├── prompt_templates.py    # Шаблоны промптов по типам
│   ├── file_parser.py         # Парсер файлов
│   └── prompt_guard.py        # Защита от prompt injection
└── db/
    ├── feedback_store.py      # Хранилище оценок
    ├── vector_store.py        # ChromaDB эталоны
    ├── secure_config.py       # Валидация .env
    └── team_store.py          # Список команд
```

---

## 🔄 Workflow

```
1. Загрузка требований (файл/текст)
   ↓
2. Автоопределение типа + выбор LLM/глубины
   ↓
3. Генерация (3 слоя: QA док → Список → Детальные кейсы)
   ↓
4. Оценка результата (принять/отклонить)
   ↓
5. Настройка XML-обвязки (проект, домен, команда)
   ↓
6. Экспорт (XML для Zephyr / CSV / Markdown)
```

---

## 📜 История изменений

### 2026-02-18
- 🧹 **Cleanup:** Удалены неиспользуемые импорты (`os`, `time`, `AuditLog`) из `app.py`
- 🐛 **Fix:** Исправлена проблема с session_state — добавлено сохранение `feature` и `selected_provider` для персистентности между перезагрузками
- 📝 **Docs:** Добавлена история изменений в README.md

### 2026-02-17
- 🧹 **Cleanup:** Удален дублирующий файл `agents/tc_formatter.py` (оставлена корневая версия)
- 🧹 **Cleanup:** Удалены временные файлы: `app_old.py`, `fix_app.py`, `write_*.py`, `update_*.py`, `cli_layered.py`
- 🐛 **Fix:** Удален editable install из `requirements.txt`
- 🧹 **Cleanup:** Очистка `__pycache__` директорий
- 🔧 **Config:** Улучшен `.gitignore` (добавлены `.claude/`, рекурсивный `__pycache__`)
- 📝 **Docs:** Заменен `CONTEXT.md` на `MEMORY.md` (проектная документация)
- ✨ **Feature:** Добавлен новый модуль `agents/layered_generator.py`
- 🚀 **Deploy:** Все изменения запушены в `origin/main`

### 2026-02-14
- ✨ **Feature:** Создан 3-слойный генератор (`layered_generator.py`)
- ✨ **Feature:** Создан менеджер промптов с автоопределением типа (`prompt_templates.py`)
- 🐛 **Fix:** Удален OpenAI из `llm_client.py`
- 🔧 **Config:** Добавлены провайдеры: GigaChat (scope=GIGACHAT_API_PERS), DeepSeek, Ollama, LM Studio
- ✨ **Feature:** Автопродолжение при обрыве XML (MAX_CONTINUATIONS=5) в `SingleCaseGenerator`

### 2024 (ранее)
- 🎉 **Initial:** Первая версия SimpleC v1.0 RAG test generator
- ✨ **Feature:** Добавлены модули защиты (prompt_guard, secure_config, audit_log)
- 🧹 **Cleanup:** Удалены старые v0 файлы (scripts, tests, configs)

---

## 📄 Лицензия

Проприетарный проект.

---

## 👥 Авторы

- **Stefan Zastylov** - разработка и поддержка

---

**Версия:** 2.0
**Последнее обновление:** 2026-02-18
